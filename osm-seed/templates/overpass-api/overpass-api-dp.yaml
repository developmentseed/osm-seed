{{- if .Values.overpassApi.enabled }}
{{- if .Values.overpassApi.persistenceDisk.enabled -}}

############## k3s: dynamic local-path (no PV) OR static hostPath when staticHostPath=true ##############
{{- if or (ne .Values.cloudProvider "k3s") (and (eq .Values.cloudProvider "k3s") .Values.overpassApi.persistenceDisk.staticHostPath) }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Release.Name }}-overpass-api-data
  labels:
    app: {{ template "osm-seed.name" . }}
    component: overpass-api
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
spec:
  ############## AWS Provider ##############
  {{- if eq .Values.cloudProvider "aws" }}
  storageClassName: ""
  capacity:
    storage: {{ .Values.overpassApi.persistenceDisk.AWS_ElasticBlockStore_size }}
  accessModes:
    - ReadWriteOnce
  awsElasticBlockStore:
    volumeID: {{ .Values.overpassApi.persistenceDisk.AWS_ElasticBlockStore_volumeID }}
    fsType: ext4
  {{- end }}

  ############## k3s static hostPath (fixed dir = data identity, reusable across releases) ##############
  {{- if and (eq .Values.cloudProvider "k3s") .Values.overpassApi.persistenceDisk.staticHostPath }}
  storageClassName: ""
  persistentVolumeReclaimPolicy: Retain
  capacity:
    storage: {{ .Values.overpassApi.persistenceDisk.localVolumeSize }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.overpassApi.persistenceDisk.localVolumeHostPath | quote }}
    type: DirectoryOrCreate
  {{- end }}
---
{{- end }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-overpass-api-data
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: {{ template "osm-seed.name" . }}
    component: overpass-api
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
spec:
  ############## k3s ##############
  {{- if eq .Values.cloudProvider "k3s" }}
  {{- if .Values.overpassApi.persistenceDisk.staticHostPath }}
  storageClassName: ""
  volumeName: {{ .Release.Name }}-overpass-api-data
  {{- else }}
  storageClassName: local-path
  {{- end }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.overpassApi.persistenceDisk.localVolumeSize }}
  {{- else }}
  ############## Other providers ##############
  storageClassName: ""
  volumeName: {{ .Release.Name }}-overpass-api-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      ############## AWS Provider ##############
      {{- if eq .Values.cloudProvider "aws" }}
      storage: {{ .Values.overpassApi.persistenceDisk.AWS_ElasticBlockStore_size }}
      {{- end }}
  {{- end }}
{{- end }}
{{- end }}
