{{- if and .Values.taginfoWeb.enabled .Values.taginfoWeb.ingress.enabled }}
{{- $ing := .Values.taginfoWeb.ingress -}}
{{- $hosts := $ing.hosts -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-taginfo-web
  labels:
    app: {{ template "osm-seed.name" . }}
    component: taginfo-web
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
  {{- with $ing.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ $ing.className | default .Values.ingressClassName }}
  {{- if $ing.tls.enabled }}
  tls:
    - hosts:
        {{- range $hosts }}
        - {{ . }}
        {{- end }}
      secretName: {{ $ing.tls.secretName | default (printf "%s-secret-taginfo" .Release.Name) }}
  {{- end }}
  rules:
    {{- range $hosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $.Release.Name }}-taginfo-web
                port:
                  number: 80
    {{- end }}
{{- end }}
