{{- if (.Values.taginfoWeb.ingress | default dict).enabled }}
{{- /* Toggle with taginfoWeb.ingress.enabled. Set taginfoWeb.ingress.{className,annotations,hosts,tls}.
       className falls back to the global ingressClassName. */ -}}
{{- $ing := .Values.taginfoWeb.ingress -}}
{{- $primary := .Values.taginfoWeb.ingressDomain | default (printf "taginfo.%s" .Values.domain) -}}
{{- $hosts := $ing.hosts | default (list $primary) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "osm-seed.fullname" . }}-ingress-taginfo-api
  {{- with $ing.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ $ing.className | default .Values.ingressClassName }}
  {{- if ($ing.tls | default dict).enabled }}
  tls:
    - hosts:
        {{- range $hosts }}
        - {{ . }}
        {{- end }}
      secretName: {{ ($ing.tls | default dict).secretName | default (printf "%s-secret-taginfo" (include "osm-seed.fullname" .)) }}
  {{- end }}
  rules:
    {{- range $hosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "osm-seed.fullname" $ }}-taginfo
                port:
                  number: 80
    {{- end }}
{{- end }}
