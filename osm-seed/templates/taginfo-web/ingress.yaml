{{- if and .Values.taginfoWeb.enabled .Values.taginfoWeb.ingress.enabled }}
{{- $ing := .Values.taginfoWeb.ingress -}}
{{- $primary := .Values.taginfoWeb.ingressDomain | default (printf "taginfo.%s" .Values.domain) -}}
{{- $hosts := $ing.hosts | default (list $primary) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "osm-seed.fullname" . }}-ingress-taginfo-api
  {{- with $ing.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ $ing.className | default .Values.ingressClassName }}
  {{- if $ing.tls.enabled }}
  tls:
    - hosts:
        {{- range $hosts }}
        - {{ . }}
        {{- end }}
      secretName: {{ $ing.tls.secretName | default (printf "%s-secret-taginfo" (include "osm-seed.fullname" .)) }}
  {{- end }}
  rules:
    {{- range $hosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "osm-seed.fullname" $ }}-taginfo
                port:
                  number: 80
    {{- end }}
{{- end }}
