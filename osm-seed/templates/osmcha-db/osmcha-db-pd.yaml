{{- if .Values.osmchaDb.enabled -}}
{{- if .Values.osmchaDb.persistenceDisk.enabled -}}

############## k3s: dynamic local-path (no PV) OR static hostPath when staticHostPath=true ##############
{{- if or (ne .Values.cloudProvider "k3s") (and (eq .Values.cloudProvider "k3s") .Values.osmchaDb.persistenceDisk.staticHostPath) }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Release.Name }}-osmcha-db-pv
  labels:
    app: {{ template "osm-seed.name" . }}
    component: osmcha-db-pv
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
spec:
  accessModes:
    - ReadWriteOnce
  ############## AWS Provider ##############
  {{- if eq .Values.cloudProvider "aws" }}
  storageClassName: ""
  capacity:
    storage: {{ .Values.osmchaDb.persistenceDisk.AWS_ElasticBlockStore_size }}
  awsElasticBlockStore:
    volumeID: {{ .Values.osmchaDb.persistenceDisk.AWS_ElasticBlockStore_volumeID }}
    fsType: ext4
  {{- end }}

  ############## k3s static hostPath (fixed dir = data identity, reusable across releases) ##############
  {{- if and (eq .Values.cloudProvider "k3s") .Values.osmchaDb.persistenceDisk.staticHostPath }}
  storageClassName: ""
  persistentVolumeReclaimPolicy: Retain
  capacity:
    storage: {{ .Values.osmchaDb.persistenceDisk.localVolumeSize }}
  hostPath:
    path: {{ .Values.osmchaDb.persistenceDisk.localVolumeHostPath | quote }}
    type: DirectoryOrCreate
  {{- end }}
---
{{- end }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-osmcha-db-pv-claim
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: {{ template "osm-seed.name" . }}
    component: osmcha-db-pvc
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
spec:
  ############## k3s ##############
  {{- if eq .Values.cloudProvider "k3s" }}
  {{- if .Values.osmchaDb.persistenceDisk.staticHostPath }}
  storageClassName: ""
  volumeName: {{ .Release.Name }}-osmcha-db-pv
  {{- else }}
  storageClassName: local-path
  {{- end }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.osmchaDb.persistenceDisk.localVolumeSize }}
  {{- else }}
  ############## Other providers ##############
  storageClassName: ""
  volumeName: {{ .Release.Name }}-osmcha-db-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      ############## AWS Provider ##############
      {{- if eq .Values.cloudProvider "aws" }}
      storage: {{ .Values.osmchaDb.persistenceDisk.AWS_ElasticBlockStore_size }}
      {{- end }}
  {{- end }}
{{- end }}
{{- end }}
