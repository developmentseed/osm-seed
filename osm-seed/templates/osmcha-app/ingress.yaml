{{- if (.Values.osmchaApi.ingress | default dict).enabled }}
{{- /* Toggle with osmchaApi.ingress.enabled. Set osmchaApi.ingress.{className,annotations,hosts,tls}.
       className falls back to the global ingressClassName. */ -}}
{{- $ing := .Values.osmchaApi.ingress -}}
{{- $primary := .Values.osmchaApi.ingressDomain | default (printf "osmcha.%s" .Values.domain) -}}
{{- $hosts := $ing.hosts | default (list $primary) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "osm-seed.fullname" . }}-ingress-osmcha-app
  {{- with $ing.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ $ing.className | default .Values.ingressClassName }}
  {{- if ($ing.tls | default dict).enabled }}
  tls:
    - hosts:
        {{- range $hosts }}
        - {{ . }}
        {{- end }}
      secretName: {{ ($ing.tls | default dict).secretName | default (printf "%s-secret-osmcha" (include "osm-seed.fullname" .)) }}
  {{- end }}
  rules:
    {{- range $hosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "osm-seed.fullname" $ }}-osmcha-app
                port:
                  number: 80
    {{- end }}
{{- end }}
