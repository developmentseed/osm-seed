{{- if .Values.nominatimDb.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-nominatim-db
  labels:
    app: {{ template "osm-seed.name" . }}
    component: nominatim-db-service
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
spec:
  ports:
    - port: {{ .Values.nominatimDb.env.PG_PORT  | quote}}
      targetPort: {{ .Values.nominatimDb.env.PG_PORT  | quote}}
  selector:
    app: {{ template "osm-seed.name" . }}
    release: {{ .Release.Name }}
    run: {{ .Release.Name }}-nominatim-db
{{- end }}