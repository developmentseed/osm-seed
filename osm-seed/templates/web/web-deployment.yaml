{{- if .Values.web.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "osm-seed.fullname" . }}-web
  labels:
    app: {{ template "osm-seed.name" . }}
    component: web-deployment
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.web.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "osm-seed.name" . }}
      release: {{ .Release.Name }}
      run: {{ .Release.Name }}-web
  template:
    metadata:
      labels:
        app: {{ template "osm-seed.name" . }}
        release: {{ .Release.Name }}
        run: {{ .Release.Name }}-web
    spec:
      {{- if .Values.web.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.web.serviceAccount.name }}
      automountServiceAccountToken: true
      {{- end }}
      priorityClassName: {{ .Release.Name }}-{{ .Values.web.priorityClass | default "high-priority" }}
      containers:
        - name: {{ .Chart.Name }}-web
          image: "{{ .Values.web.image.name }}:{{ .Values.web.image.tag }}"
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            {{- if .Values.web.livenessProbeExec }}
            exec:
              command:
              - /bin/bash
              - -c
              - ./liveness.sh
            initialDelaySeconds: 120
            timeoutSeconds: 5
            periodSeconds: 20
            failureThreshold: 5
            successThreshold: 1
            {{- else }}
            httpGet:
              path: /api/0.6/capabilities
              port: 80
            initialDelaySeconds: 600 # 10 min, because the compile process takes time.
            timeoutSeconds: 30
            {{- end }}
          {{- if and .Values.web.resources.enabled (or .Values.web.resources.requests.enabled .Values.web.resources.limits.enabled) }}
          resources:
            {{- if .Values.web.resources.requests.enabled }}
            requests:
              memory: {{ .Values.web.resources.requests.memory }}
              cpu: {{ .Values.web.resources.requests.cpu }}
            {{- end }}
            {{- if .Values.web.resources.limits.enabled }}
            limits:
              memory: {{ .Values.web.resources.limits.memory }}
              cpu: {{ .Values.web.resources.limits.cpu }}
            {{- end }}
          {{- end }}
          env:
            # ---- Computed / cluster-specific (not plain web.env pass-throughs) ----
            - name: GET_HOSTS_FROM
              value: dns
            - name: POSTGRES_HOST
              value: {{ .Release.Name }}-db
            - name: POSTGRES_DB
              value: {{ .Values.db.env.POSTGRES_DB }}
            - name: POSTGRES_PASSWORD
              value: {{ quote .Values.db.env.POSTGRES_PASSWORD }}
            - name: POSTGRES_USER
              value: {{ .Values.db.env.POSTGRES_USER }}
            - name: OPENSTREETMAP_memcache_servers
              value: {{ .Release.Name }}-memcached:11211
            # cgimap always runs as its own pod (external)
            - name: EXTERNAL_CGIMAP
              value: "true"
            - name: CGIMAP_URL
              value: {{ .Release.Name }}-cgimap-service
            - name: CGIMAP_PORT
              value: {{ quote 80 }}
            # ---- Everything else passes straight through from web.env ----
            {{- range $k, $v := .Values.web.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          volumeMounts:
            - mountPath: /dev/shm
              name: shared-memory
      volumes:
        - emptyDir:
            medium: Memory
            sizeLimit: {{ .Values.web.sharedMemorySize }}
          name: shared-memory
      {{- if .Values.web.nodeSelector.enabled }}
      nodeSelector:
        {{ .Values.web.nodeSelector.label_key }} : {{ .Values.web.nodeSelector.label_value }}
      {{- end }}
      # Node affinity
      {{- if .Values.web.nodeAffinity.enabled }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: {{ .Values.web.nodeAffinity.key }}
                    operator: In
                    values:
                    {{- range .Values.web.nodeAffinity.values }}
                      - {{ . | quote }}
                    {{- end }}
        # Pod anti-affinity, to set that no two pods are scheduled on the same node
        {{- if .Values.web.podAntiAffinity.enabled }}
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: {{ template "osm-seed.name" . }}
                  component: web-deployment
              topologyKey: "kubernetes.io/hostname"
        {{- end }}
      {{- end }}
{{- end }}
