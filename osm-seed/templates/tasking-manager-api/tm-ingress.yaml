{{- if (.Values.tmApi.ingress | default dict).enabled }}
{{- /* Toggle with tmApi.ingress.enabled. Set tmApi.ingress.{className,annotations,hosts,tls}.
       className falls back to the global ingressClassName. */ -}}
{{- $ing := .Values.tmApi.ingress -}}
{{- $primary := .Values.tmApi.ingressDomain | default (printf "tm-api.%s" .Values.domain) -}}
{{- $hosts := $ing.hosts | default (list $primary) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "osm-seed.fullname" . }}-ingress-tm-api
  {{- with $ing.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ $ing.className | default .Values.ingressClassName }}
  {{- if ($ing.tls | default dict).enabled }}
  tls:
    - hosts:
        {{- range $hosts }}
        - {{ . }}
        {{- end }}
      secretName: {{ ($ing.tls | default dict).secretName | default (printf "%s-secret-tm-api" (include "osm-seed.fullname" .)) }}
  {{- end }}
  rules:
    {{- range $hosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $.Release.Name }}-tasking-manager-api
                port:
                  number: 80
    {{- end }}
{{- end }}
